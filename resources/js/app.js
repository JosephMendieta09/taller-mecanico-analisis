/* =============================================================
   EL GARAGE — Punto de entrada JS
   Vite empaqueta todo lo importado aquí en un solo bundle.
   ============================================================= */
 
import './sidebar.js';
 
/* Por vista: importar aquí o cargar con @push('scripts') + @vite()
   en cada blade según convenga.
   Ejemplo:
   import './dashboard.js';
*/